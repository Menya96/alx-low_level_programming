#include "main.h"

/**
* _islower -> checks if the character is lowercase
* Description: checks if the character is lowercase
* Return: returns 1 or 0 depending on conditions
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
